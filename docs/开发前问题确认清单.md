# 资讯颗粒化项目 —— 开发前问题确认清单

> 以下问题需要在正式编码前逐一确认，避免开发中途返工。

---

## 一、输入与数据源

### Q1：输入方式确认
MVP 阶段只支持手工粘贴全文和导入 txt/md 文件，是否还需要支持其他格式？
- [ ] 仅 txt / md / 手工粘贴
- [ ] 需要支持 PDF 导入
- [x] 需要支持 URL 抓取（简单版，仅抓正文）
- [ ] 需要支持 HTML 文件导入

### Q2：文档来源类型确认
目前定义的 source_type 包括 news / annual_report / wechat / research / pdf / manual。是否需要增减？实际测试会用到哪些类型的文章？

暂时不确定，很多资料测试之后才知道，需要保留新增的口子

### Q3：测试语料
是否已经准备好 10 篇用于 Milestone 2 小样本验证的典型行业文章？如果有，请确认：
- 涵盖哪些行业/细分领域？
  - 精细化工、汽车涂料、风电涂料、船舶涂料和其他制造行业

- 是否覆盖了 7 个第一批 fact_type？
  - 覆盖

- 文章平均长度大约多少字？
  - 200-8000


### Q4：文章语言
是否需要支持纯英文文章抽取，还是只处理中文/中英混合文本？

中文居多，需要可以抽取英文

---

## 二、文本切分

### Q5：切分策略选择
注意事项文档提到了三种切分策略（短文直通 / 段落组块 / 主题块拆），是否全部实现，还是 MVP 只做前两种？
- [ ] 全部实现
- [x] 只做短文直通 + 段落组块（推荐 MVP）
- [ ] 只做段落组块

### Q6：chunk 大小参数
建议值是 500~900 字（理想）、1200~1500 字（上限），overlap 50~120 字。这些参数是写死还是可配置？

可以根据实际开发情况决定

### Q7：清洗逻辑范围
注意事项强调"先清洗再拆分"。MVP 阶段的清洗需要做到什么程度？
- [x] 只做基础去空行、去多余空格
- [x] 需要去广告、导航、免责声明（需提供规则或模板）
- [x] 需要处理 HTML 标签清洗
- [ ] 暂时不做清洗，假设输入已经是干净正文

---

## 三、LLM 模型选择

### Q8：模型供应商
三个 Agent 分别使用什么模型？请确认：
- Evidence Finder（便宜稳定）：____________
- Fact Extractor（高质量）：____________
- Reviewer / Validator：____________

可选项参考：
- OpenAI GPT-4o / GPT-4o-mini
- Anthropic Claude Sonnet / Haiku
- DeepSeek【都使用这个】
- 通义千问 / 智谱 / 百度文心
- 本地模型

### Q9：API 调用方式
- [x] 直接调用官方 API
- [ ] 通过 OpenAI 兼容接口（如 one-api / new-api 等中转）
- [ ] 通过内部统一网关
- [ ] 需要支持多个 provider 切换

### Q10：API Key 管理
API Key 如何提供？
- [ ] 环境变量
- [x] 配置文件（.env）
- [ ] 系统需要提供 Key 管理界面
- [ ] 其他方式

### Q11：Token 成本控制
是否需要在 MVP 阶段追踪 token 消耗？`extraction_task` 表有 input_tokens / output_tokens 字段，是否启用？
- [x] 需要，每次调用记录 token 数
- [ ] MVP 暂不记录，后续再加

---

## 四、fact_type 与抽取规则

### Q12：第一批 fact_type 确认
确认 MVP 只做以下 7 类，暂不做其余 7 类：
1. FINANCIAL_METRIC
2. SALES_VOLUME
3. CAPACITY
4. INVESTMENT
5. EXPANSION
6. MARKET_SHARE
7. COMPETITIVE_RANKING

是否需要调整？比如某些业务场景需要优先加入 COOPERATION 或 PRICE_CHANGE？

​	需要

### Q13：predicate 枚举
每个 fact_type 下的 predicate 是否需要预定义枚举白名单，还是允许模型自由生成？
- [x] 预定义白名单（更稳定，需要维护）
- [x] 允许自由生成但做后校验
- [ ] 提供推荐列表但不强制

### Q14：qualifiers 白名单
每个 fact_type 的 qualifiers 是否强制按白名单限制？
- [ ] 强制白名单（模型输出白名单外的 key 会被丢弃）
- [x] 推荐白名单 + 允许额外字段
- [ ] 不限制

### Q15：confidence 阈值
模型输出的 confidence 分数，是否设置准入门槛？
- [x] 不设门槛，交给 Reviewer 判断
- [ ] 设置下限（建议值：______）
- [ ] confidence < 某值自动进人工池

---

## 五、实体标准化

### Q16：初始实体种子库
MVP 是否需要预置一批种子实体（如行业头部公司、常见品牌名）？
- [ ] 需要，请提供种子清单或来源
- [x] 不需要，全部从抽取中动态积累
- [ ] 需要少量核心实体（约 _____ 个）

### Q17：实体类型范围
完整方案定义了 12 种 entity_type（COMPANY, BRAND, PRODUCT, PERSON, ORG, LOCATION, POLICY, EVENT, METRIC, MATERIAL, PLANT, VEHICLE_MODEL）。MVP 是否全部保留？
- [x] 全部保留
- [ ] 只做 COMPANY + BRAND + LOCATION（最小集）
- [ ] 自定义范围：____________

### Q18：实体匹配未命中策略
当 subject / object 无法匹配到已有 entity 时：
- [x] 保留原文，不创建新实体（推荐 MVP）
- [ ] 自动创建候选实体，状态为"待确认"
- [ ] 需要人工确认后才创建

---

## 六、审核机制

### Q19：人工审核交互方式
MVP 阶段人工审核以什么形式进行？
- [ ] 命令行导出 CSV/JSON，人工标注后导回
- [x] 简单 Web 表格界面（需要额外开发）
- [ ] 直接在数据库中操作（SQLite 工具）
- [ ] 先不做人工审核，全部自动

### Q20：审核人
MVP 阶段由谁负责人工审核？是否只有一个审核员？
- [x] 单人审核
- [ ] 多人审核（需要权限管理）
- [ ] 暂时不区分审核人

### Q21：UNCERTAIN 处理策略
当 Reviewer 输出 UNCERTAIN 时，除了进入人工池，是否有其他处理？
- [x] 仅进人工池等待审核
- [ ] 允许重新送一次 Extractor（用不同 prompt 或更高级模型）
- [ ] 直接丢弃

---

## 七、查询与输出

### Q22：查询交互方式
MVP 阶段的查询以什么方式提供？
- [ ] Python 脚本 + 命令行参数
- [x] Jupyter Notebook
- [ ] 简单 CLI 菜单界面
- [ ] FastAPI 接口（提前做轻 API）

### Q23：导出格式
查询结果是否需要导出？支持什么格式？
- [ ] 仅终端显示
- [x] 导出 CSV
- [ ] 导出 JSON
- [ ] 导出 Excel
- [ ] 多种格式可选

### Q24：是否需要简单统计
MVP 是否需要基础统计功能？
- [ ] 不需要，只要能查询即可
- [x] 需要：每篇文档抽取数量统计
- [x] 需要：按 fact_type 分布统计
- [x] 需要：按审核状态分布统计

---

## 八、工程与运行环境

### Q25：Python 版本
- [ ] Python 3.10+
- [ ] Python 3.11+
- [x] Python 3.12+
- [ ] 无特别要求

### Q26：依赖管理
- [x] pip + requirements.txt
- [ ] Poetry
- [ ] PDM
- [ ] uv

### Q27：ORM 选择
数据库操作是否使用 ORM？
- [x] 不使用 ORM，直接 SQL（最轻量，推荐 MVP）
- [ ] 使用 SQLAlchemy
- [ ] 使用 Peewee
- [ ] 其他：____________

### Q28：运行环境
系统将在什么环境下运行？
- [x] 本地 Windows 开发机
- [ ] 本地 Mac
- [x] Linux 服务器
- [x] Docker 容器
- [x] 需要跨平台兼容

### Q29：日志与错误处理
- [x] 使用 Python logging 标准库
- [ ] 使用 loguru
- [ ] MVP 暂时 print 即可
- [x] 需要日志文件持久化

### Q30：配置管理
chunk 大小、模型名称、API Key、审核阈值等参数如何管理？
- [ ] 写死在代码里（最快但不灵活）
- [ ] .env 文件 + python-dotenv
- [x] config.yaml / config.toml
- [ ] 命令行参数

---

## 九、边界与特殊情况

### Q31：同一文档重复导入
如果同一篇文章被导入两次（content_hash 相同），系统应该：
- [ ] 直接拒绝，提示已存在
- [x] 覆盖更新
- [ ] 保留两份，标记重复

### Q32：空抽取结果处理
如果一篇文档完全没有抽出 evidence 或 fact_atom：
- [x] 正常记录，文档状态标记为"无事实"
- [ ] 给出提示但不做特殊处理
- [x] 需要人工确认是否文章不适合抽取

### Q33：超长文章处理
如果文章超过 5000 字甚至更长（如研报），是否有特殊处理？
- [x] 正常按规则切分，无上限
- [ ] 设置文章长度上限，超出截断
- [ ] 设置文章长度上限，超出拒绝

### Q34：表格/列表数据
资讯中经常有表格或列表形式的数据，MVP 是否需要处理？
- [ ] 暂不处理表格，只处理正文段落
- [x] 需要基础表格识别和抽取
- [ ] 需要详细表格解析能力

### Q35：多篇文章批量导入
MVP 是否需要支持批量导入（如一次导入一个文件夹）？
- [ ] 不需要，逐篇导入
- [ ] 需要简单批量导入能力
- [x] 需要批量导入 + 进度显示

---

## 十、后续演进预留

### Q36：后续 API 化计划
MVP 脚本跑通后，是否需要立即包一层 API？
- [x] MVP 验证通过后立即做
- [ ] 视情况决定
- [ ] 暂时不考虑

### Q37：前端需求
是否有前端需求的时间预期？
- [ ] MVP 阶段完全不需要前端
- [x] MVP 后 1~2 个月内需要简单前端
- [ ] 暂无前端计划

### Q38：数据持久化与备份
SQLite 数据文件是否需要定期备份？
- [x] 手动备份即可
- [ ] 需要自动备份脚本
- [ ] 暂不考虑

---

## 使用说明

1. 在每个问题后的选项中打 `[x]` 标记你的选择，或在空白处填写具体内容
2. 对于不确定的问题，可以标注"待定"并在开发过程中逐步明确
3. 建议至少确认 **Q1、Q3、Q8、Q12、Q19、Q22、Q25、Q28** 这 8 个核心问题后再开始编码
4. 本清单可在开发过程中持续更新
